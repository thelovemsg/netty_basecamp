# Todo — 2026-04-13

## 완료

### Fare / FarePolicy CRUD 구현
- [x] `FareRepository`, `FarePolicyRepository` 인터페이스 수정
  - `save()` 반환 타입 `void` → 엔티티 반환으로 변경
  - `deleteById()` 추가 (`FareRepository`)
  - `findById()`, `deleteById()` 추가 (`FarePolicyRepository`)
- [x] `InMemoryFareRepository` 신규 생성 (`domains/fare/infrastructure/`)
- [x] `InMemoryFarePolicyRepository` 신규 생성 (`domains/fare/infrastructure/`)
- [x] `FareApplicationService` 신규 생성 — Fare CRUD (findById, create, delete)
- [x] `FarePolicyApplicationService` 신규 생성 — Policy CRUD + Fare 존재 검증
- [x] `FareController` 신규 생성 — Fare CRUD 핸들러
- [x] `FarePolicyController` 신규 생성 — Policy CRUD 핸들러 (FareController에서 분리)
- [x] `FareRouteConfig` 신규 생성 — DI 조립 + 라우트 등록
- [x] `AppConfig`에 `FareRouteConfig` 연결

**확정된 API**
```
GET    /api/fares/{id}
POST   /api/fares                              { "name": "...", "basePrice": 10000 }
DELETE /api/fares/{id}
GET    /api/fares/{fareId}/policies
GET    /api/fares/{fareId}/policies/{policyId}
POST   /api/fares/{fareId}/policies            { "type": "FIXED_AMOUNT_DISCOUNT", "value": 1000, "basis": "ORIGINAL", "priority": 1 }
PUT    /api/fares/{fareId}/policies/{policyId} { "value": 1000, "basis": "ORIGINAL", "priority": 1 }
DELETE /api/fares/{fareId}/policies/{policyId}
```

**설계 결정 사항**
- `FareController` / `FarePolicyController` 분리 → ApplicationService도 동일하게 분리
- Controller 내부에 Jackson 역직렬화용 Request record 선언 (도메인에 Jackson 의존 금지 규칙 준수)
  - `FareCreateRequest(name, basePrice)` — `Money` Jackson 역직렬화 불가로 BigDecimal 수신 후 변환
  - `FarePolicyCreateRequest`, `FarePolicyUpdateRequest` — 다중 인자 생성자 역직렬화 불가 동일 이유
- `FarePolicyApplicationService`는 `FareRepository`도 의존 — Policy 조작 시 Fare 존재 검증 필요

---

### CouponIssueApplicationService 요금 추적 기능 추가
- [x] `FareCalculationDomainService.calculateFinalPrice()` → `calculate()` 로 변경
  - 기존: `Money` 반환 (컨텍스트 버림)
  - 변경: `FareCalculationContext` 전체 반환 (originFare, originalPrice, appliedPolicyDescriptions 포함)
- [x] `CouponIssueResult` 신규 생성 (`domains/coupon/application/`)
  - `issuedCoupon`, `fareId`, `fareName`, `originalPrice`, `appliedPrice`, `appliedPolicyDescriptions`
- [x] `CouponIssueApplicationService.issueCoupon()` 반환 타입 `IssuedCoupon` → `CouponIssueResult`
- [x] `CouponIssueApplicationServiceTest` 반환 타입 변경에 맞춰 수정

---

### 테스트 코드 작성
- [x] `FakeFareRepository` 인터페이스 변경에 맞춰 수정 (save 반환, deleteById 추가)
- [x] `FakeFarePolicyRepository` 인터페이스 변경에 맞춰 수정 (findById, deleteById, save 반환 추가)
- [x] `FareApplicationServiceTest` 신규 작성 (9개)
  - findById: 성공 / 없는 id 예외
  - create: id 자동 부여 / 필드 저장 / 초기 상태 ACTIVE / 시간 기록 / 다중 생성 id 구분
  - delete: 삭제 후 조회 예외 / 없는 id 예외
- [x] `FarePolicyApplicationServiceTest` 신규 작성 (14개)
  - findByFareId: 해당 정책만 반환 / 빈 리스트 / 없는 fareId 예외
  - findById: 성공 / 없는 policyId 예외 / 다른 fareId 예외
  - add: id 자동 부여 / 필드 저장 / 다중 추가 id 구분 / 없는 fareId 예외
  - update: 필드 수정 / modifiedAt 갱신 / 다른 fareId 예외
  - delete: 삭제 후 조회 예외 / 다른 정책 유지 / 다른 fareId 예외
- [x] 전체 108개 테스트 통과 (실패 0, 에러 0)

---

### ADR-009 구현 — EventLoop 블로킹 대응 (Virtual Thread 오프로드)
- [x] `HttpRoutingHandler` — Virtual Thread 오프로드 적용
  - `VIRTUAL_EXECUTOR = Executors.newVirtualThreadPerTaskExecutor()` 선언
  - ByteBuf 복사(`request.content().toString()`)는 EventLoop 스레드에서 완료 후 `RequestContext` 빌드
  - 블로킹 가능한 `handle()` 이후 로직만 `VIRTUAL_EXECUTOR.submit()` 으로 오프로드
- [x] `CustomChannelInitializer` — 미사용 import 정리
- [x] `log4j2.properties` — 로그 설정 개선
  - 로그 파일명 패턴에서 타임스탬프 제거 (`${date:yyyyMMddHHmmss}_` 삭제)
  - `rootLogger.type = AsyncRoot` 추가 — Disruptor 기반 비동기 루트 로거 활성화

---

### ADR 추가
- [x] ADR-010: ApplicationService에서 여러 Repository + DomainService를 함께 쓰는 것은 정상
  - Coupon 발급 시 Fare 도메인이 엮이는 이유, ApplicationService 레이어 책임 정리

---

## 다음 할 일

- [ ] Coupon CRUD API 구현 (CouponApplicationService, InMemoryCouponRepository, CouponController, CouponRouteConfig)
- [ ] CouponIssueApplicationService HTTP 엔드포인트 노출 (`POST /api/coupons/{couponId}/issue`)
- [ ] Phase 2: RDBMS 비관적/낙관적 락 동시성 제어 준비
